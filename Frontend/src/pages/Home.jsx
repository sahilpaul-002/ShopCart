import React, {useContext} from 'react'
import GetUserContext from '../contexts/GetUserContext';

export default function Home() {
  // const {user} = useContext(GetUserContext);
  // console.log(user);

  return (
    <div className="home-container w-[100vw] h-[100vh]  bg-gradient-to-l from-[#141414] to-[#0c2025]">
      home
    </div>
  )
}
